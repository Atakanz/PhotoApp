import {StyleSheet} from "react-native";

export default StyleSheet.create({
    image:{
        width:300,
        height:300,
    },
    container: {
        flex: 1,
        justifyContent: 'center',
        alignItems: 'center',
    }
});